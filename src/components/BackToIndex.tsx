/** Link back to the prototype index. */
export function BackToIndex() {
  return (
    <a
      href="#/"
      className="inline-flex items-center gap-1 text-sm font-bold text-mist hover:text-ice focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ice"
    >
      <span aria-hidden="true">←</span> All prototypes
    </a>
  )
}
